---
alwaysApply: false
---
# 项目名称

海悦推拿（上门按摩微信小程序）

---

# 你的角色

你是资深产品经理 + 系统架构师 + Laravel + 微信小程序全栈工程师。

你的任务：

理解项目业务，
自主拆解模块，
生成 spec，
设计数据库，
生成代码，
并保证整个项目长期可维护。

你必须像真正的研发团队一样思考。

必须：

先分析
→ 再拆模块
→ 再生成 spec
→ 再开发。

---

# 项目定位

这是一个：

基于代理二维码获客、
用户自主点技师、
后台人工调度的上门按摩微信小程序。

核心特点：

1 用户极简下单
2 用户自主选技师
3 代理二维码推广
4 二级代理分佣
5 后台人工运营调度
6 平台统一管理

本质：

人工调度型服务平台。

不是外卖系统。

不是自动派单系统。

---

# 业务角色

系统包含：

1 用户端（微信小程序web）
2 代理体系
3 平台管理后台

无独立技师端。

技师由后台管理。

---

# 核心业务流程

用户：

代理二维码进入
→ 微信授权登录
→ 浏览技师
→ 选择服务项目
→ 选择时长
→ 填写地址
→ 选择预约时间
→ 微信支付/余额支付
→ 订单生成
→ 后台实时提醒
→ 后台人工确认
→ 服务完成
→ 用户评价/售后

---

# 代理业务规则

采用：

每次扫码按当前代理归属。

不锁定用户。

即：

用户每次扫码进入：

当前订单归属当前二维码代理。


后台可配置：

- 独立代理佣金

佣金规则：

订单完成后生成。

退款自动失效。

---

# 技师规则

无技师小程序。

用户：

自主点技师。

后台：

人工协调。

技师状态：

- 可预约
- 服务中
- 休息中
- 停止接单

用户只能预约：

可服务技师。

---

# 支付体系

支持：

1 微信支付
2 余额支付
3 优惠券

余额：

支持充值赠送。

例如：

充500送50。

余额：

不可提现。

仅消费。

---

# 后台核心

后台负责：

订单
技师
代理
佣金
提现
营销
风控
售后

后台必须：

支付成功实时提醒。

形式：

- 红点
- 声音
- 弹窗

避免漏单。

---

# 用户端模块

必须包含：

1 登录
2 首页
   - 顶部广告 Banner 轮播（多图自动轮播，无跳转，无 Banner 时不渲染）
3 技师列表
4 技师详情
   - 技师信息区展示圆形头像，无头像时降级为默认头像
   - 服务区分「项目推荐」/「客户评价」两个 Tab
   - 项目推荐 Tab：服务项目以封面图卡片布局展示，支持数量加减（仅 UI，不影响下单）
   - 点击封面图进入服务项目详情页
   - 客户评价 Tab：懒加载评价列表，支持分页「加载更多」，接口失败静默展示「暂无评价」
5 服务项目详情页
   - 展示全宽详情图（无详情图时降级展示封面图）
   - 固定底部「立即下单」按钮，跳转下单页并携带 therapist_id 与 service_id
7 支付
8 订单中心
9 评价投诉
10 余额
11 优惠券
12 个人中心

目标：

极简下单。

---

# 代理模块

必须包含：

1 专属二维码
2 订单查看
3 佣金记录
4 提现申请
5 数据看板

仅可查看自身数据。

---

# 后台模块

必须包含：

1 Dashboard
2 订单中心
3 技师管理
4 代理管理
5 服务项目
   - 支持列表封面图（cover）与详情页图片（detail_image）独立上传管理
   - detail_image 为空时，用户端详情页自动降级展示封面图
6 财务管理
7 营销管理
8 风控与合规
9 操作日志
10 内容管理
   - Banner 管理（图片上传、标题、排序、启用/禁用）

---

# Cursor工作规则

你必须：

先阅读项目需求。

然后：

自主完成：

1 模块拆解
2 spec生成
3 数据库设计
4 API设计
5 页面结构
6 代码实现

每个模块：

必须：

先输出：

spec

等待确认。

然后：

再生成代码。

禁止：

未经分析直接写代码。

请先输出：

《项目模块拆解 + 开发路线 + spec清单》

暂时不要写代码。